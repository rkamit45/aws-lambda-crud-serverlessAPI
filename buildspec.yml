version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17

  pre_build:
    commands:
      - java -version
      - mvn -version

  build:
    commands:
      - echo "Building application shaded JAR..."
      - mvn clean package -DskipTests

  post_build:
    commands:
      - echo "Deploying target/course-service.jar to AWS Lambda..."
      - aws lambda update-function-code --function-name course-service --zip-file fileb://target/course-service.jar
      - echo "Deployment complete!"

artifacts:
  files:
    - target/course-service.jar
    - template.yaml
  discard-paths: yes
